version: '3.8'

services:
  backend:
    image: backend
    build:
      context: ./backend
    ports:
      - "3001:3001"
    networks:
      - app-network
    volumes:
      - shared-uploads:/usr/src/app/build/uploads

networks:
  app-network:
    driver: bridge

volumes:
  shared-uploads:
